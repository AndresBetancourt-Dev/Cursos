import { introduceMyself } from "..";

describe("Introduce Myself", () => {
  it("Should Introduce Me", () => {
    expect(introduceMyself("J", "H")).toEqual("Hello J H");
  });

  it("Should", () => {
    expect(1).toEqual(1);
  });
});
